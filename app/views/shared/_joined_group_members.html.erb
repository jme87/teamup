<h5>Group members</h5>
<div class="members-avatars d-flex flex-wrap justify-content-between">
  <% @users.each do |user| %>
    <%= cl_image_tag user.photo.key, class: "each-member-avatar mb-2", width: 60, height: 60, crop: :thumb, gravity: :face %>
  <% end %>
</div>
